{
  "swagger": "2.0",
  "info": {"title": "validate must not rewrite the caller's document", "version": "1.0.0"},
  "paths": {
    "/things": {
      "get": {
        "parameters": [{"name": "body", "in": "body", "schema": {"$ref": "#/definitions/Bag"}}],
        "responses": {"200": {"description": "ok"}}
      }
    }
  },
  "definitions": {
    "Bag": {
      "type": "object",
      "additionalProperties": {"$ref": "#/definitions/Item"},
      "default": {"anything": {"name": "x"}}
    },
    "Item": {"type": "object", "properties": {"name": {"type": "string"}}}
  }
}
